import { createTheme, MantineGradient } from '@mantine/core';

export const theme = createTheme({
  primaryColor: 'blue',
});

export const appGradient: MantineGradient = { from: 'cyan', to: 'indigo', deg: 135 };
